---
# tasks file for at
- name: install at software
  package:
    name: at
    state: "{{ at_package_state }}"

- name: start and enable at software
  service:
    name: atd
    state: started
    enabled: yes
  when:
    - ansible_virtualization_type != "docker" or at_ignore_docker
